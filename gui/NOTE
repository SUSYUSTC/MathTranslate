Upgrade mathtranslate to latest before compilation

Only works for some python + kivy versions
Successful test on python 3.9 and kivy 2.2.0

in kivy source code kivy/logger.py, comment line 609-611
```
console = ConsoleHandler()
console.setFormatter(formatter)
logger.addHandler(console)
```
